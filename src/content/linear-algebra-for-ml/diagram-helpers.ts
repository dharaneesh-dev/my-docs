// Shared across every content chapter — see @/lib/diagram-helpers. Re-exported here so the
// existing relative imports inside this chapter's diagram files keep working unchanged.
export * from "@/lib/diagram-helpers";
