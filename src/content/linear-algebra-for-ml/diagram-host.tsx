// Shared across every content chapter — see @/components/docs/diagram-host. Re-exported here so
// the existing relative imports inside this chapter's diagram files keep working unchanged.
export { DiagramHost } from "@/components/docs/diagram-host";
export type { DiagramRender } from "@/components/docs/diagram-host";
